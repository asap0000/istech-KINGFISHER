<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base theme; the actual UI styling is driven by Compose Material3. -->
    <style name="Theme.PrivacyCamera" parent="android:Theme.Material.NoActionBar">
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:windowBackground">@android:color/black</item>
    </style>
</resources>
